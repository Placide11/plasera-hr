# SQLite (default, zero-setup local development)
DATABASE_URL="dev.db"

# Postgres example (production / Vercel deployment)
# DATABASE_URL="postgresql://user:password@host:5432/plasera_hr?schema=public"
